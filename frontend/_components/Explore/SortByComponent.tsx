'use client';
import React from 'react'
import { IUser } from '@/app/types'
type Props = {
    users:IUser[]
}

export default function SortByComponent({users}: Props) {

  return (
    <div>
        
    </div>
  )
}